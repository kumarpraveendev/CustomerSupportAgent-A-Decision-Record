"""Reference slice for a customer-support agent.

The differentiated layer from the decision record: the verdict gate
(ADR-0002), the cost router (ADR-0003), and the eval harness (ADR-0005).
Pure standard library — no API keys, no network — so it runs anywhere.
"""
__version__ = "0.1.0"
