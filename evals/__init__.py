"""Evaluation harness: deterministic safety invariants + a golden-case runner.

This is the gate from decisions/0005-evaluation-gate.md. The safety invariants
are deterministic on purpose — "the judge model thought it was fine" is not the
assurance we want on whether the agent gave away money.
"""
